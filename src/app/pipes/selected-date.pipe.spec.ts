import { SelectedDatePipe } from './selected-date.pipe';

describe('SelectedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
